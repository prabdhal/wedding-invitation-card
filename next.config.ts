/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you need to add external images or domains
  images: {
    domains: [], // Add external image domains if needed
  },
};

module.exports = nextConfig;
